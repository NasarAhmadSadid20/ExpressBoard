
           <div class="main2">
           <% if(typeof user != "undefiend"){ %>
           <% post.posts.forEach(element => { %>
           <div class="post-list">
           <div class="post">
           <img src="">
           <p> <%=element.content %> </p>
           </div>
           </div>
           <% })}; %>